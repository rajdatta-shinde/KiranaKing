{
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "inngest": "npm run inngest:dev --prefix server",
    "dev": "concurrently -n server,client,inngest -c blue,green,magenta \"npm run server\" \"npm run client\" \"npm run inngest\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}